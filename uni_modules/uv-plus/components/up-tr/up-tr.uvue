<template>
	<view class="up-tr">
		<slot></slot>
	</view>
</template>

<script>
	import { propsTr } from './props';
	import { mpMixin } from '../../libs/mixin/mpMixin';
	import { mixin } from '../../libs/mixin/mixin';
	/**
	 * Tr  
	 * @description 
	 * @tutorial url
	 * @property {String}
	 * @event {Function}
	 * @example
	 */
	export default {
		name: 'up-tr',
		mixins: [mpMixin, mixin, propsTr],
		data() {
			return {
				
			}
		}
	}
</script>

<style lang="scss" scoped>
	.up-tr {
		@include flex;
	}
</style>
